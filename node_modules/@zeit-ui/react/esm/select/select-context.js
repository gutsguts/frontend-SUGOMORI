import React from 'react';
var defaultContext = {
  visible: false,
  size: 'medium',
  disableAll: false
};
export var SelectContext = React.createContext(defaultContext);
export var useSelectContext = function useSelectContext() {
  return React.useContext(SelectContext);
};