import React from 'react';
var defaultContext = {
  size: 'medium',
  type: 'default',
  auto: false,
  disabled: false,
  loading: false
};
export var ButtonDropdownContext = React.createContext(defaultContext);
export var useButtonDropdown = function useButtonDropdown() {
  return React.useContext(ButtonDropdownContext);
};