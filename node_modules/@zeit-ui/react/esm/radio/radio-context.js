import React from 'react';
var defaultContext = {
  disabledAll: false,
  inGroup: false
};
export var RadioContext = React.createContext(defaultContext);
export var useRadioContext = function useRadioContext() {
  return React.useContext(RadioContext);
};