import React from 'react';
var defaultContext = {
  values: []
};
export var CollapseContext = React.createContext(defaultContext);
export var useCollapseContext = function useCollapseContext() {
  return React.useContext(CollapseContext);
};