import React from 'react';
var defaultContext = {
  columns: []
};
export var TableContext = React.createContext(defaultContext);
export var useTableContext = function useTableContext() {
  return React.useContext(TableContext);
};