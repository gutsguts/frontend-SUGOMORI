import React from 'react';
var defaultContext = {};
export var ModalContext = React.createContext(defaultContext);
export var useModalContext = function useModalContext() {
  return React.useContext(ModalContext);
};