import React from 'react';
var defaultContext = {
  inGroup: false
};
export var TabsContext = React.createContext(defaultContext);
export var useTabsContext = function useTabsContext() {
  return React.useContext(TabsContext);
};