import { ZeitUIThemes } from 'components/styles/themes';
export interface SelectSize {
    height: string;
    fontSize: string;
    minWidth: string;
}
export declare const getSizes: (theme: ZeitUIThemes, size?: "mini" | "small" | "medium" | "large" | undefined) => SelectSize;
